# Blue3 Cloud — Deployment queue

> Snapshot of **2026-09-24**. This directory holds **what is still to be
> deployed**. What is already implemented lives in [`docs/`](../docs/). When a
> front is finished, it moves from here to there.
>
> New snapshot in `README_YYYYMMDD.md`; never edit a previous one.
> Previous: [README_20260813.md](README_20260813.md).

## State

- **Fork back on the lab nodes.** On 2026-09-21, an `apt upgrade` reverted
  b3pve1 and b3pve2 to the stock `pve-manager 9.2.20`. The fork was merged onto
  upstream 9.2.20, rebuilt, and reinstalled. Both nodes now run
  `pve-manager/9.2.20+blue3.1/cdabba49` with `pve-manager` on `apt-mark hold`.
  Procedure: [`docs/upstream-sync.md`](../docs/upstream-sync.md).
- **The cluster has a third member: `b3p1`** (`100.64.65.160`, physical, runs
  the B3DEV-WORK and AI-BENCHMARK guests). It got the same `9.2.20+blue3.1`
  build on the same day, also on hold. All three nodes now serve Blue3 Cloud.
- Access: `ssh root@100.64.100.254` (b3pve1) · `root@100.64.100.253` (b3pve2) ·
  `root@100.64.65.160` (b3p1).
- Rechecked on 2026-09-24: nothing from the hardening runbook is applied on
  any node. No node has `/etc/default/pveproxy` or `wg-mgmt`. `fail2ban`
  runs without the Blue3 jail on b3pve1 and b3pve2, and is not installed on
  b3p1.

---

## Queue

### 1. Apply the hardening runbook — BLOCKS PRODUCTION

Unchanged since 2026-08-13. See the previous snapshot for the order and what is
still to rehearse: WireGuard, internal DNS, ACME DNS-01 (needs the Cloudflare
token), TOTP, step 7 + `verify.sh`, and fail2ban regex. With b3p1 in the
cluster, `preflight.sh` has to be rerun: `ALLOW_FROM` now needs
`100.64.65.160` too.

### 2. Settle the domain of the UI links — BLOCKS RELEASE

`www/manager6/Utils.js:36-38` still points at `blue3.com.br`. These are
unconfirmed placeholders.

### 3. Per-panel help buttons

Unchanged. 156 files use `onlineHelp:`. The widget toolkit is installed on the
lab nodes to inspect the help API.

### 4. What to do with `blue3-logos/`

Unchanged.

### 5. Multi-tenancy — pick a path

Unchanged. See [`docs/multi-tenancy.md`](../docs/multi-tenancy.md).

### 6. Second corosync link

Unchanged. `ens19` on b3pve1 is still `DOWN`.

### 7. A fork apt repository — NEW, optional

Today the `.deb` is copied by hand and pinned with `apt-mark hold`. A small
signed apt repo for `blue3.<N>` builds would turn installs into
`apt install`. The hold would still be needed, since the Proxmox repo keeps
offering newer upstream versions. Worth it only if the fork reaches more nodes
than it does today.

---

## Suggested next step

Rerun `preflight.sh` with three nodes, then rehearse the runbook on the lab
(item 1).
